{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Jupyter",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "justMyCode": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}